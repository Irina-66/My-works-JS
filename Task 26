В этом задании вам необходимо выполнить возведение переменной a в степень b и возврат значения из функции.
function testMath(a, b) {
 var z = Math.pow(a, b)
 return(z);// Тут нужно написать решение
}
